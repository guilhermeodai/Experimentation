using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Aula6_Variaveis : MonoBehaviour {

	// SINTAXE
	// tipo de acesso (opcional) + tipo de variável + nome da variável = valor da variável (opcional)
	// na nomenclatura de variáveis, inicia-se com letra minúscula e os termos seguintes com maiúscula. Ex.: nomeComposto
	// não são permitidos números e caracteres especiais no 1º caractere, exceto o underscore

	public int inteiro;
	public float flutuante;
	public string texto;
	public bool booleano;
	int _novaVariavel;

	// Use this for initialization
	void Start () {
		
	}
	
	// Update is called once per frame
	void Update () {
		
	}
}